import React, { FC } from "react";

const ProductsListScreen: FC = (): JSX.Element => {
  return (
    <div className="App">
      <header className="App-header">
        <p>ProductsListScreen</p>
      </header>
    </div>
  );
};

export default ProductsListScreen;
