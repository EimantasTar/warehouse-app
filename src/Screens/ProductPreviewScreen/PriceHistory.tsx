import React from 'react';

const PriceHistory = ({ id }: { id: string }): JSX.Element => {
    return (
        <div>
            <p>PriceHistory ID: {id}</p>
        </div>
    )
};

export default PriceHistory;
